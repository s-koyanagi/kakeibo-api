spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:dev;MODE=PostgreSQL;DB_CLOSE_ON_EXIT=TRUE
    username: dev
    password: dev
  h2:
    console:
      enabled: true
      path: /h2-console
      setting:
        web-allow-others: true